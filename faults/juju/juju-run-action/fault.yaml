---
apiVersion: litmuschaos.io/v1alpha1
description:
  message: |
    Executes a Juju action on a unit to test action handling behavior.
kind: ChaosExperiment
metadata:
  name: juju-run-action
  labels:
    name: juju-run-action
    app.kubernetes.io/part-of: litmus
    app.kubernetes.io/component: chaosexperiment
    app.kubernetes.io/version: ci
spec:
  definition:
    scope: Cluster
    permissions:
      - apiGroups: [""]
        resources: ["pods"]
        verbs:
          [
            "create",
            "delete",
            "get",
            "list",
            "patch",
            "update",
            "deletecollection",
          ]
      - apiGroups: [""]
        resources: ["events"]
        verbs: ["create", "get", "list", "patch", "update"]
      - apiGroups: [""]
        resources: ["secrets", "configmaps"]
        verbs: ["get", "list"]
      - apiGroups: [""]
        resources: ["pods/log"]
        verbs: ["get", "list", "watch"]
      - apiGroups: [""]
        resources: ["pods/exec"]
        verbs: ["get", "list", "create"]
      - apiGroups: ["batch"]
        resources: ["jobs"]
        verbs: ["create", "list", "get", "delete", "deletecollection"]
      - apiGroups: ["litmuschaos.io"]
        resources: ["chaosengines", "chaosexperiments", "chaosresults"]
        verbs: ["create", "list", "get", "patch", "update", "delete"]
      - apiGroups: [""]
        resources: ["nodes"]
        verbs: ["get", "list"]
    image: "juju-go-runner:latest"
    imagePullPolicy: IfNotPresent
    args:
      - -c
      - ./experiments -name juju-run-action
    command:
      - /bin/bash
    env:
      - name: TOTAL_CHAOS_DURATION
        value: "30"

      - name: RAMP_TIME
        value: ""

      - name: JUJU_MODEL_UUID
        value: ""

      - name: UNIT
        value: ""

      - name: ACTION_NAME
        value: ""

      # JSON string of action parameters
      - name: ACTION_PARAMS
        value: ""

      - name: JUJU_CREDENTIALS_FILE
        value: "/tmp/juju-credentials.yaml"

    labels:
      name: juju-run-action
      app.kubernetes.io/part-of: litmus
      app.kubernetes.io/component: experiment-job
      app.kubernetes.io/version: ci
    secrets:
      - name: juju-credentials
        mountPath: /tmp/
